# Plan B Foundation GitHub Pages

Placeholder Vite website for `luganoplanb.github.io`, using a local copy of the
shared Plan B Vite theme.

## Commands

```sh
npm install
npm run dev
npm run build
```

The theme source lives in `src/theme` so the site can build independently.
